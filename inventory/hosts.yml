---
all:
  children:
    netbox:
      hosts:
        netbox01:
          ansible_host: 192.168.1.10   # change to your target host
          ansible_user: ansible
    linux_servers:
      hosts: {}  # populate with servers to inventory into NetBox
